trigger:
  branches:
    include:
      - main  # Change this to your desired branch

pool:
  name: 'tzl-poc'
  demands:
  - agent.name -equals tzl-poc-ado-afk-6c74f4d8c9-nt47g

steps:
  
- task: Kubernetes@1
  inputs:
    connectionType: 'Kubernetes Service Connection'
    kubernetesServiceEndpoint: 'taastzlpoc'
    namespace: 'default'
    command: 'get'
    arguments: 'pods'
    secretType: 'dockerRegistry'
    containerRegistryType: 'Azure Container Registry'
    versionSpec: '1.27.3'
#    secretType: 'dockerRegistry'
#    containerRegistryType: 'Azure Container Registry'
#    secretType: 'dockerRegistry'
#    containerRegistryType: 'Azure Container Registry'
#- task: Docker@2
#  displayName: 'Build and push Docker image'
#  inputs:
#    command: 'buildAndPush'
#    containerRegistry: 'dockerafk'  # Use the service connection name you created earlier
#    repository: 'alperanfk/cicd'  # Replace with your Docker Hub username and image repository name
#    Dockerfile: '**/Dockerfile'  # Path to your Dockerfile
